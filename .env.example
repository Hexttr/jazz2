# Пароль админки (по умолчанию 513277). Сгенерировать: node scripts/hash-password.js 513277
ADMIN_PASSWORD_HASH=$2b$12$jvHeKyEQiAtW0B0D6cDQ8uTUifoFcHkXBJ3o0/h7rsKC.CcJR4BK6
# Секрет для подписи сессии (замените на случайную строку в проде)
SESSION_SECRET=your-secret-at-least-32-chars

# Хранилище контента (меню, разделы сайта). Достаточно одного варианта:
# 1) Redis (рекомендуется): Vercel → Storage → Connect Store → Upstash Redis.
#    Переменные подставятся автоматически: UPSTASH_REDIS_REST_URL, UPSTASH_REDIS_REST_TOKEN
# 2) Blob: для контента и загрузки картинок (BLOB_READ_WRITE_TOKEN из Storage → Blob)
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=
# BLOB_READ_WRITE_TOKEN=

# Уведомления в Telegram при новой заявке на бронирование: создайте бота через @BotFather, вставьте токен. ID получателя задаётся в админке (раздел «Бронирование»).
# TELEGRAM_BOT_TOKEN=
